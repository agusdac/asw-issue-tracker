<!DOCTYPE html>
<html>
  <head>
    <title>Issue Tracker ASW - Bitbucket style</title>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="wrap">
	<!-- The sidebar -->
	<div class="sidebar">
	  <%= link_to 'Home', issues_path, :class=>"active" %>
	  <% if current_user %>
          <%= link_to (image_tag(current_user.imageurl, :class => "perfil_image")), 'users/'+current_user.id.to_s%>
          <p id="perfil_name">Signed in as <strong><%= current_user.name %></p>
          
          <%= link_to "Sign out", signout_path, id: "sign_out" %>
          <% else %>
          <%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in" %>
          <% end %>
	</div>
    	<div class="body">
    	<%= yield %>
    	</div>
    </div>
  </body>
</html>
