
<div class="container">
    <div class="row">
    <% if current_user %>
    <div class="col-2">

          <img class="perfil_image" src ="<%=current_user.imageurl%>">
          </div>

    <div class="col-10">
<%= form_for([@issue, @issue.comments.build], html: { multipart: true}) do |f| %>
<%= f.text_area :content%>

    <!-- Other fields go here... -->
    <%= f.label :file, "File" %>
    <%= f.file_field :file %>

<br>
<%= f.submit%>
<% end %>
<% end %>
    </div>
    </div>
</div>